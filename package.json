{
  "name": "phone-repair-website",
  "version": "1.0.0",
  "description": "A phone repair website with image uploads and product listings.",
  "main": "server.js",
  "scripts": {
  "start": "node server.js",
  "build": "npm run build",
  "test": "echo \"Error: no test specified\" && exit 1",
  },
  "dependencies": {
    "express": "^4.17.1",
    "multer": "^1.4.2",
    "cors": "^2.8.5",
    "socket.io": "^4.0.0"
  },
  "engines": {
    "node": "16.x"  
  }
}
